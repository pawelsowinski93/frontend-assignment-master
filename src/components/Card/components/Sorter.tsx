import React from "react";
import { FaSort } from "react-icons/fa";

const Sorter: React.FC = () => (
  <div className="card__sort">
    <div id="section-title">Sort by date</div>
    <FaSort />
  </div>
);

export default Sorter;
